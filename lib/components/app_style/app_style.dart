import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

class AppStyles {
  // Định nghĩa base style chung cho các TextStyle
  static TextStyle baseTextStyle = GoogleFonts.roboto(
    fontWeight: FontWeight.w700, // Độ dày chung
    height: 1.2, // Tỉ lệ dòng chữ chung
  );

  // Định nghĩa TextStyle cho tiêu đề lớn (title)
}
